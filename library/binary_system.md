---
id: 260316-binary_system
tags:
  - computer_theory
created: 2026-03-16
status: draft
---
# Binary System
It's the default system computers use to perform all sort of tasks.

It uses **Bits** to represents numbers. Each individual bit can be either 0 (unset) or 1 (set).

In the binary system, each bit is a power of 2 from right to left we have:
```plaintext
first bit is 2 ^ 0
second bit is 2 ^ 1
third bit is 2 ^ 2
...
nth bit is 2 ^ n
```
## Units of digital data
- **Bit (b)**: Individual 0 or 1
- **Byte (B)**: Group of 8 b
- **Kilobyte (KB)**: 1024 B
- **Megabyte (MB)**: 1024 KB
- **Gigabyte (GB)**: 1024 MB
- **Terabyte (TB)**: 1024 GB
- **Petabyte (PT)**: 1025 TB
## Why do we use the binary system?
We use bits because of the underlying physical system that relies on transistors. A transistor can only be powered on (1) or powered off (0). Each transistor represents a bit and that's why computers only use the binary system to perform every calculation.
## Examples
Representation of a integer using a byte:
```plaintext
The representation of 53 using the binary system is: 00110101

We have the following bits set: 0, 2, 4, and 5
The number is decimal is then: 
2 ^ 0 + 2 ^ 2 + 2 ^ 4 + 2 ^ 5 = 1 + 4 + 16 + 32 = 53
```
## References
### Connects with
- [Two's complement](two_complement.md): Sign number representation system.
- [Bit manipulation](bit_manipulation.md): Operations on individual bits.
## TL;DR
The binary system is the default system utilized by computer to perform any sorts of calculations. The numbers generated by it are the base of all the abstractions that generate all sorts of stuff in a computer screen.

Each bit is a power of 2 starting from 0 up to n.
## Flashcards
- Q: What is the binary system and how does it work?
- A: The binary system utilizes 0 and 1 to count, each bit is a power of 2 starting from 2.
- Q: Why do we use the binary system?
- A: The binary system is used because of underlying transistor technology.
- Q: What are the binary units? And their respective sizes?
- A: bit (0 or 1), B(1024 b), KB(1024 B), MG(1024 KB), GB(1024 MB), TB(1024 GB), PB(1024 TB)